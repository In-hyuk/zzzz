230821
함수 복습
F12 눌르면 그 정의된 함수로 이동
-----------------------------
함수 중요
함수 연습
---------------------------
함수 연습
---------------------------
함수 연습 계속
-----------------------------------------------------------------------.
230822
배열 변수를 매개변수로 전달하기(함수)
함수 호출할때 배열 주소값? 이용 ex) show_arr(arr);
Call by Address
함수 만들때는 배열 크기가 얼마든 간에 빈칸으로
ex) void show_arr(int a[]){}
------------------------------
값에 의한 전달 은 함수 실행 한번하고나면 끝 리셋됨. 소멸.
주소에 의한 전달 은 주소값을 던져줌 실행하고 나면 공간 안의 값 바뀜 
------------------------------
배열 변수를 이용한 함수 생성 실습
------------------------------
지역변수 -> 함수 안에서만 사용
전역변수 -> 함수 밖
전역변수 쓰면 디버깅(추적) 어려움. 될수있으면 매개변수 쓰기
 ------------------------------
함수 복습
-------------------------------
배열 출력하는 함수 작성 실습 계속
-------------------------------------------------------------------------
230823
함수 복습
실전에서는 하드코딩은 금지 ex) show_arr(arr1, 2, 3);
하드코딩된 값들은 상수 변수(const) 또는 #define을 사용
상수변수는 대부분 대문자로 표기
c언어 난수생성하기
#include <stdlib.h> -> srand,rand
#include<time.h> -> time
rand, srand, time함수 필요
------------------------------
실습및 복습
함수 중요
---------------------------------------------------------------------------
230824
로또 번호 중복제거 생성 실습
-------------------------------
배열을 이용해 성적구하기 실습
-------------------------------
포인터 
pnum : 주소값
*pnum : 주소값의 들어있는 값

포인터 변수의 초기화
int* pnum = NULL;

포인터 변수 p에 배열변수 arr의 주소값을 참조할때
int* p =NULL;
p=arr;
p를 배열처럼 쓸 수 있음 p[i] 이런식.
p[0] = arr[0]
p[1] = arr[1] ....

*(p+0) = arr[0]  = *p
*(p+1) = arr[1]
*(p+2) = arr[2]   ....
---------------------------------------------------------------------------
230825
포인터 변수 : 주소를 참조하는 변수
포인터 더 깊게 들어간다면...
<void*, 함수포인터, 메모리 동적 할당, 포인터배열, 이중포인터>
--------------------------
구조체
C언어 구조체 -----> 클래스 확장(자바,C#,C++,Python....)
--------------------------
복습할 때 중점적으로 볼 것.
1. 변수
2. 제어문
3. 반복문
4. 배열
5. 함수
6. 구조체
--------------------------
scanf 쓰면 문자열 공백은 처리되지 않음
ex)대구시 동구 -> 대구시동구 사이에 공백은 처리되지않음. 대구시 로만 저장됨

















